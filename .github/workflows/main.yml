name: Main pipeline

on:
  push:
    branches:
      - feature/add-pipelines

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
          
      - name: Run dir command
        run: dir

      - name: Run echo command
        run: echo hello

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1

      - name: Build
        shell: cmd
        run: msbuild VinaText.sln /p:Configuration=Release /p:Platform="Any CPU"